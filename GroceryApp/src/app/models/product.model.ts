export interface ProductModel {
  name: string;
  weight ? : string;
  size ? : string;
  price ? : string;
  brand ? : string;
  uid ? : string; // user id
  cid ? : string; // category id
  _id ? : string;
}
